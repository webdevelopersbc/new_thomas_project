import { createUpcomingEventsQuery, createPreviousEventsQuery } from '@utils';

export const QUERY_UPCOMING_EVENTS = createUpcomingEventsQuery();
export const QUERY_PREVIOUS_EVENTS = createPreviousEventsQuery();
